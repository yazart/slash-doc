# Slash Doc

A VS Code extension that opens a Shoelace-powered webview with an Editor.js document editor.

## Development

```sh
npm install
npm run compile
```

Open the project in VS Code and run the extension host. Then execute `Slash Doc: Open Editor` from the command palette.
